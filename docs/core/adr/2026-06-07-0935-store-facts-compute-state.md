# Store facts, compute state

Gorchestra core stores durable facts and computes lifecycle/display state from those facts wherever practical. This applies across Portfolio orchestration concepts such as Goals, Plans, Deliveries, Slices, Current Memories, Links, Executions, Actions, Missions, and Decisions; stored state should be used only when the state itself is an irreducible fact rather than a derivable summary.
