# Server Consumer TODO

Durable follow-up items that are known but intentionally deferred.

## Project configuration

- Add a Project Config page at `/projects/:projectId/config` so Project-scoped Model References can navigate to the concrete configuration surface and users can edit Project Config without relying on lower-level APIs.
