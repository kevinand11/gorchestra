# Verified email Authentication Identity linking

Future Google and GitHub Authentication Identities may auto-link to an existing User when the provider asserts a verified email address that matches an existing Email Authentication Identity. We chose verified-email auto-linking for lower-friction sign-in across Authentication Methods, accepting that provider integrations must treat unverified or absent provider emails as insufficient for linking.
